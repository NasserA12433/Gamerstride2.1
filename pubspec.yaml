name: gamerstrideflutter2_2
description: "A new Flutter project."
version: '1.0.1'
environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  circular_countdown_timer: ^0.2.3
  flutter_local_notifications: ^17.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "^0.13.1"
  flutter_lints: ^3.0.2

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "icon.png"

flutter:
  uses-material-design: true